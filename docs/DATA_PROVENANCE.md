# Data provenance

`data/reported/` contains raw CSV/JSON outputs available in the packaging session.

`data/user_reported/preregistered_depth64_summary.*` contains values transcribed from the terminal output supplied by the researcher. It is explicitly marked as user-reported because the full raw depth-64 output bundle was not present in the packaging environment. Replace it with the original output CSV/JSON files when available.
